export enum FolderContentType {
  File,
  Folder
}

export enum StringEncoding {
  UTF8 = "utf8",
  UTF16LE = "utf16le",
  UTF16BE = "utf16be",
  ShiftJIS = "shiftjis",
}

export enum Endianness {
  LittleEndian,
  BigEndian,
}